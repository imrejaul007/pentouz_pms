import dotenv from "dotenv"; dotenv.config(); import mongoose from "mongoose"; mongoose.connect(process.env.MONGO_URI).then(() => { mongoose.connection.db.collection("bookings").dropIndex("source_1_channelBookingId_1").then(() => { console.log("✅ Dropped problematic index"); process.exit(0); }).catch(() => { console.log("ℹ️  Index already removed or doesnt exist"); process.exit(0); }); });
